import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    'flx-center': {
        margin: 20,
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
})